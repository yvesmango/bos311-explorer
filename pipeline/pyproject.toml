[project]
name = "bos311-explorer"
version = "0.1.0"
description = "Boston 311 civic data pipeline for the BOS311 Explorer portfolio repo"
requires-python = ">=3.12"
dependencies = [
    "psycopg2-binary>=2.9.12",
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
]
